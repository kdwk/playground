[package]
name = "tcp_server"
version = "0.1.0"
edition = "2024"

[dependencies]
documents = "0.1.2"
main = { path = "../main" }
serde = { version = "1.0.218", features = ["derive"] }
serde_json = "1.0.140"
